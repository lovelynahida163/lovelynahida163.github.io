---
layout: post
title: "GNOT"
date: 2026-04-06
categories: Learning
---

GNOT（General Neural Operator Transformer）是近年提出的基于 Transformer 的通用算子架构。它的核心思想是：**用交叉注意力机制直接建模函数‑查询点之间的交互**，把算子学习看作一个序列到序列的映射问题。

核心公式：

`h = CrossAttn( Q=query_pos, K=func_features, V=func_features )`

具体展开：

- 输入函数 `u` 在 `m` 个传感器位置采样得到 `{(x_i, u(x_i))}`，经过一个编码器变成 key/value 序列。
- 想要查询的输出位置 `y` 经过位置编码后作为 query。
- 交叉注意力计算 query 与每个 key 的相似度，加权聚合 value，得到该位置的特征。
- 最后经过 MLP 输出 `G(u)(y)`。

GNOT 可以同时处理多个输入函数（比如多个物理场）——只需要把不同函数的特征拼起来作为 key/value。它也不要求输入函数的采样点与输出位置在同一个网格上，天然支持任意查询点。

与 DeepONet 相比，GNOT 的注意力机制是**自适应**的：每个查询点会动态关注不同的输入位置，而不是像 DeepONet 那样用固定的基函数组合。这使得 GNOT 在强非线性、多尺度、多物理场耦合问题上表现更好。

适用场景：多物理场耦合（比如流‑固‑热）、参数化 PDE、跨域泛化（训练时在一个几何上，预测另一个几何）。

缺点：Transformer 的计算量较大（`O(m * q)`，m 是输入采样点数，q 是查询点数）。对于高分辨率的大规模问题，可能比 FNO 慢。另外需要较多的训练数据才能发挥注意力的优势。

GNOT 的结构是基于 **Transformer 的交叉注意力**，专门设计用于处理任意位置的查询和多个输入函数。

**输入**：
- 输入函数的传感器位置和值：`{(x_i, u(x_i))}`，`i=1..m`。每个 `u(x_i)` 可以是向量。
- 想要查询的输出位置：`{y_j}`，`j=1..q`。

**第一步：编码输入函数**  
将每个传感器点编码为一个向量：  
`z_i = MLP_enc( [x_i; u(x_i)] )`，得到输入序列 `Z_in`，形状 `(m, d_model)`。同时加上位置编码（比如用正弦函数或可学习的嵌入）。

**第二步：交叉注意力层**  
对于每个查询位置 `y_j`，计算其输出特征：
- 查询向量：`q_j = MLP_query(y_j)`（也可能加上位置编码）。
- 键值来自输入序列：`K = Z_in * W_K`，`V = Z_in * W_V`。
- 注意力分数：`α_{j,i} = softmax_i( q_j · K_i / √d )`。
- 聚合：`c_j = Σ_i α_{j,i} * V_i`。
- 最终特征：`h_j = MLP_out( c_j )`（可能包含残差和层归一化）。

可以堆叠多个交叉注意力层（每一层再用相同的 `Z_in` 或使用上一层的输出作为新的查询特征）。

**第三步：输出**  
`G(u)(y_j) = MLP_output( h_j )`，输出维度为 `d_out`。

**扩展**：如果有多个输入函数（比如速度场和压力场），只需要把它们的所有传感器点都放入 `Z_in` 即可。

一句话：**多输入、多物理、强非线性，GNOT 很能打**。
